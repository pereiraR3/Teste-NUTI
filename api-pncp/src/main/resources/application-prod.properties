# Configurações do Servidor
server.error.include-stacktrace=never

# Configurações Spring
spring.main.allow-circular-references=true

# Configurações de Logging
logging.level.org.springframework=DEBUG

# Configuração do DataSource para PostgreSQL no RDS
spring.datasource.url=jdbc:postgresql://databasenuti.cfccgg0kg3fc.us-east-1.rds.amazonaws.com:5432/testenuti
spring.datasource.username=projetoNuti
spring.datasource.password=zaqxsw122123
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do Flyway para migração de banco de dados
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
logging.level.org.flywaydb=DEBUG

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurações do SpringDoc para documentação de API
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
